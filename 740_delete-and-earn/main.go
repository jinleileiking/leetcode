package main

func deleteAndEarn(nums []int) int {
	parsed := make(map[int]bool)

	for _, v := range nums {
		if ok := parsed[v]; ok {
			continue
		}

	}

}

func pick(nums []int) int {

	func pick(nums []int) int {
	}
}
