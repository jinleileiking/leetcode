package main

import (
	"testing"

	"github.com/davecgh/go-spew/spew"
)

func TestMain(t *testing.T) {

	// spew.Dump(countPrimes(0))
	// spew.Dump(countPrimes(1))
	// spew.Dump(countPrimes(2))
	spew.Dump(countPrimes(3))
	// spew.Dump(countPrimes(10))
}
